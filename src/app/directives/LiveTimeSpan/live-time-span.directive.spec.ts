import { LiveTimeSpanDirective } from './live-time-span.directive';

describe('LiveTimeSpanDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveTimeSpanDirective();
    expect(directive).toBeTruthy();
  });
});
