import { TimeSpanPipe } from './time-span.pipe';

describe('TimeSpanPipePipe', () => {
    it('create an instance', () => {
        const pipe = new TimeSpanPipe();
        expect(pipe).toBeTruthy();
    });
});
